# Audit fonctionnel prod — 16 juillet 2026

Convention de nommage des données de test (jamais routable, jamais confondu avec un vrai compte) :
- Email : `test-16juillet-{role}-{n}@book-n-pay.invalid` (domaine `.invalid`, réservé IANA, ne délivrera jamais)
- Nom affiché : préfixé `[TEST 16/07]`
- Business de test : `businesses.name` préfixé `[TEST 16/07]`

## Comptes créés

- `test-16juillet-client-1@book-n-pay.invalid` — role client, `id` `8d9edcc3-b141-43ce-bae2-cc52452236d1` (créé 16/07, toujours en base à ce jour).
- `test-16juillet-pro-1@book-n-pay.invalid` — role pro, business `[TEST 16/07] Institut Audit Connexion` — créé et **supprimé le 17/07** dans la même session de test (voir "Parcours testés" ci-dessous). Plus en base.

## Parcours testés

### 17/07 — Connexion espace pro (Playwright, réel, contre prod `www.book-n-pay.com`)
Compte pro de test créé directement via `service_role` (auth.users + business publié + business_settings `subscription_status=active` avec `stripe_subscription_id` factice, pour sauter onboarding/setup-billing et tester uniquement le flow de connexion). Scripts jetables (`create-test-pro.mjs`/`test-connexion-pro.mjs`/`cleanup-test-pro.mjs`), non conservés dans le repo.

**6/6 checks PASS** :
1. `GET /pro` sans session → redirige vers `/connexion?redirect=/pro`.
2. Mauvais mot de passe → message d'erreur affiché, reste sur `/connexion`.
3. Bon mot de passe → `signInWithPassword` OK, redirigé vers `/pro`.
4. Dashboard affiche bien le nom du business du compte connecté.
5. Session persiste après reload.

Compte + business supprimés immédiatement après (script de nettoyage ciblé par email/nom exact, pas de `LIKE` large), vérifié zéro orphelin (`app_users`/`businesses`). **Flow de connexion espace pro confirmé fonctionnel en prod, aucun bug trouvé.**

### 17/07 — Parcours client (réservation + paiement réel Stripe test, Playwright, réel, contre prod)
Prérequis : plus de compte pro utilisable (celui du test connexion avait été supprimé) → création d'une **fixture pro PERMANENTE** (`[FIXTURE PERMANENTE] Institut Audit`, slug `fixture-pro-audit`) directement via `service_role`, publiée, 1 service ("Prestation Test Fixture" 30€/dépôt 10€), 0 staff (happy path uniquement, planning hors scope). Détail complet + identifiants dans la mémoire projet (`project_bnp_fixture_pro_audit`), **ne jamais la supprimer dans un nettoyage de test**. `mode_test_paiement` revérifié `true` avant tout paiement.

Compte client jetable créé (`test-17juillet-client-parcours@book-n-pay.invalid`), parcours déroulé de bout en bout : connexion → fiche `fixture-pro-audit` → sélection service → date/créneau → étape paiement solo → **vrai Stripe Checkout hosted (mode test)** avec carte `4242 4242 4242 4242` → retour `/confirmation`.

**10/10 checks PASS** :
1-8. Connexion, fiche établissement, sélection service, étape date/heure, sélection date, créneau libre disponible, bouton Continuer actif, étape paiement atteinte.
9. Redirigé vers `checkout.stripe.com` (session `cs_test_...`, confirme le mode test respecté côté route `/api/stripe/checkout`).
10. Retour sur `/confirmation?booking=...` avec contenu affiché.

**Vérifié en base après coup (pas juste le redirect front)** : `booking_members.status='paid'`, `deposit=10`, `qr_code` généré, `stripe_payment_intent_id`/`stripe_checkout_session_id` réels posés — **le webhook Stripe a bien traité `checkout.session.completed`**, pas juste le retour visuel. `bookings.status` passe à `'completed'` dès que tous les membres actifs sont payés (comportement existant du webhook, pas introduit par ce test — à garder en tête si un jour on confond ce statut avec "prestation rendue" : il signifie en réalité "tous les dépôts payés").

Booking de test + compte client nettoyés immédiatement après (script ciblé par ID exact), fixture pro vérifiée intacte après nettoyage. **Parcours client (réservation + paiement) confirmé fonctionnel en prod, aucun bug trouvé.**

## Réservations créées

(aucune réservation de test résiduelle — toutes créées pendant les tests ci-dessus ont été nettoyées après vérification)

## Transactions Stripe test

- `cs_test_b1uu1dOpxmu80EYKaVQ2cUgFYxtj3LuycCxZBwx3ZlcfqbxTxhNxjZZsoa` / `pi_3TuE9YH9uyyHEYIO1JFQRsb8` — 17/07, parcours client, 10€ dépôt, **mode test confirmé** (`mode_test_paiement=true` vérifié avant lancement, clé `STRIPE_TEST_SECRET_KEY` utilisée par la route). Carte test `4242...`.

**⚠️ Rappel pour la suite de l'audit** : le mode de paiement est piloté par `app_config.mode_test_paiement` (voir `.env.example` et `STATUS.md`). Si ce flag passe à `false` en prod, toute réservation créée pendant l'audit déclenchera un **vrai** paiement Stripe live. Revérifier la valeur actuelle avant chaque nouveau test payant, pas après.

## 17/07 — Volet fixtures pro multi-métiers (dashboard vs besoins réels par métier)

Objectif : une fixture pro PERMANENTE par secteur réel (11 secteurs, mapping détaillé dans la mémoire projet `project_bnp_fixtures_metiers`), onboardée via l'UI réelle (pas de seed direct staff/services/schedules), pour évaluer si le dashboard répond aux besoins de chaque métier. Mot de passe de test commun à toutes ces fixtures, noté en mémoire (jamais dans le code).

### Pilote — `fixture-pro-coiffure` (multi-staff, acompte+solde, cumule le plus d'axes)

**🔴 Finding priorisé — Stripe Connect bloque TOUT pro réel derrière la clé live** : `src/app/api/stripe/connect-onboarding/route.ts:30` utilise `STRIPE_SECRET_KEY || STRIPE_TEST_SECRET_KEY` (ne respecte pas `mode_test_paiement`, contrairement à `stripe/checkout` et `bookings/solde-checkout` qui le font correctement) — ET `src/app/api/pro/publish/route.ts:33` **exige** `stripe_onboarding_complete=true` pour publier un établissement. Conséquence : aujourd'hui, **aucun pro ne peut publier son établissement sans passer par un vrai compte Stripe Connect en mode live**, quel que soit `mode_test_paiement`. Vu le blocage légal en cours (radiation micro-entreprise, RDV CCI Bayonne non fait), ce n'est pas juste un risque théorique — c'est un blocage business actif pour tout onboarding réel dès aujourd'hui. À corriger avant tout vrai partenariat (faire respecter `mode_test_paiement` sur cette route, ou établir un mode `stripe_onboarding_complete` "à valider plus tard" pour ne pas bloquer la publication).

**Confirmé structurellement (pas juste suspecté)** : `StepEtablissement.tsx` (étape 1 onboarding) n'a **aucun champ ville/type** — seuls `open_time/close_time/open_days/instagram/facebook/website` sont envoyés à `/api/pro/update-profile`. Le bug latent "`city=''` à vie pour tout vrai pro" (item 8 backlog, jusqu'ici déduit du code d'approbation admin) est donc confirmé aussi côté onboarding : littéralement aucun chemin de l'app ne permet à un pro de renseigner sa ville.

**Testé et fonctionnel (via UI réelle, vérifié en base)** :
- Onboarding étape 1 (horaires/jours) + étape 2 (prestations, `PrestationsManager`) : OK.
- Équipe multi-staff (`/pro/equipe`) : ajout de 2 praticiens + configuration horaires individuels (`staff_schedules`) — vérifié en base, lignes correctes par jour.
- Parcours client sur fixture multi-staff : étape "Choisir un praticien" affichée correctement (Julien/Sarah + "Pas de préférence"), praticien choisi correctement propagé jusqu'à la réservation (`bookings.staff_id`/`staff_name` corrects), paiement acompte Stripe test → webhook → `booking_members.status='paid'`. 14/14 checks Playwright PASS.

**Reporté — `solde-checkout`** : nécessite un compte Stripe Connect configuré (`stripe_account_id`+`stripe_onboarding_complete`, `isStripeAccountConfigured` dans `solde-checkout.ts` — comportement voulu, pas un bug). Un compte Connect **TEST** a été créé pour cette fixture directement via l'API Stripe (`STRIPE_TEST_SECRET_KEY`, en dehors de la route app cassée) mais compléter le KYC test déclenche un **CAPTCHA anti-bot dès que Playwright pilote le clic** — refusé de le contourner par script. Décision Pierre : traiter ce point manuellement plus tard, en une fois pour toutes les fixtures qui en ont besoin, pas fixture par fixture. Détail dans la mémoire projet.

**Données de test nettoyées** (booking + compte client jetable), fixture `fixture-pro-coiffure` (avec ses 2 praticiens et sa prestation) vérifiée intacte et conservée pour la suite du chantier.

---

## Nettoyage — à exécuter dans Supabase SQL Editor après l'audit

Schéma réel (`src/lib/database.types.ts`, confirmé contre les migrations) :
- `app_users` n'a **pas** de colonne `email` — l'email vit uniquement dans `auth.users` (Supabase Auth), lié par `app_users.id = auth.users.id` (trigger `handle_new_user`, `supabase/migrations/0010_fix_handle_new_user_trigger.sql`). Toute identification par email doit donc passer par une jointure/sous-requête sur `auth.users`.
- `bookings` n'a pas de colonne `user_id` — la colonne réelle est `client_id` (+ `client_email`/`client_name` dénormalisés). Le template fourni utilisait `user_id`, corrigé ci-dessous.
- Aucun flag `is_test`/`is_demo` n'existe sur `app_users` ni `businesses` aujourd'hui. Identification proposée pour ce nettoyage : `auth.users.email LIKE '%@book-n-pay.invalid'` (comptes) + `businesses.name LIKE '[TEST 16/07]%'` (business). Robuste tant que la convention du jour est respectée à la lettre.
- FK cascade réelles connues avec certitude (migrations présentes dans le repo) : `staff_schedules` et `staff_absences` sont `ON DELETE CASCADE` depuis `staff`/`businesses` — pas besoin de les cibler explicitement, mais je les inclus quand même ci-dessous par prudence (no-op si déjà supprimées en cascade). Pour le reste des tables (`bookings`, `booking_members`, `chat_messages`, `booking_logs`, `services`, `business_photos`, `business_settings`, `business_reviews`, `favorites`, `referral_events`, `overage_charges`, `staff`) : la migration d'origine (`0001_schema.sql`) est absente du repo, donc le comportement CASCADE réel n'est **pas vérifiable depuis le code** — le script ci-dessous supprime explicitement dans l'ordre des dépendances (enfants avant parents), ce qui fonctionne que CASCADE soit défini ou non.

```sql
-- ══════════════════════════════════════════════════════════════════
-- Nettoyage audit fonctionnel prod — 16 juillet 2026
-- Identification : email @book-n-pay.invalid (auth.users) + business
-- préfixé [TEST 16/07]. Ordre : enfants avant parents.
-- ══════════════════════════════════════════════════════════════════

-- 1. IDs de test à nettoyer (calculés une fois, réutilisés partout)
WITH test_user_ids AS (
  SELECT id FROM auth.users WHERE email LIKE '%@book-n-pay.invalid'
),
test_biz_ids AS (
  SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%'
),
test_booking_ids AS (
  SELECT id FROM bookings
  WHERE biz_id IN (SELECT id FROM test_biz_ids)
     OR client_id IN (SELECT id FROM test_user_ids)
)
SELECT
  (SELECT count(*) FROM test_user_ids)    AS nb_users,
  (SELECT count(*) FROM test_biz_ids)     AS nb_business,
  (SELECT count(*) FROM test_booking_ids) AS nb_bookings;
-- ⚠️ Lancer d'abord ce SELECT seul et vérifier que les chiffres sont
-- cohérents avec ce qui a été créé pendant l'audit AVANT de lancer les
-- DELETE ci-dessous.

-- 2. Enfants de bookings
DELETE FROM chat_messages
WHERE booking_id IN (
  SELECT id FROM bookings
  WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%')
     OR client_id IN (SELECT id FROM auth.users WHERE email LIKE '%@book-n-pay.invalid')
);

DELETE FROM booking_logs
WHERE booking_id IN (
  SELECT id FROM bookings
  WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%')
     OR client_id IN (SELECT id FROM auth.users WHERE email LIKE '%@book-n-pay.invalid')
);

DELETE FROM booking_members
WHERE booking_id IN (
  SELECT id FROM bookings
  WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%')
     OR client_id IN (SELECT id FROM auth.users WHERE email LIKE '%@book-n-pay.invalid')
);

DELETE FROM overage_charges
WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%')
   OR booking_id IN (
     SELECT id FROM bookings
     WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%')
        OR client_id IN (SELECT id FROM auth.users WHERE email LIKE '%@book-n-pay.invalid')
   );

DELETE FROM flash_slots
WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%')
   OR booking_id IN (
     SELECT id FROM bookings
     WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%')
        OR client_id IN (SELECT id FROM auth.users WHERE email LIKE '%@book-n-pay.invalid')
   );

-- 3. bookings elle-même (après ses enfants)
DELETE FROM bookings
WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%')
   OR client_id IN (SELECT id FROM auth.users WHERE email LIKE '%@book-n-pay.invalid');

-- 4. Dépendances directes des comptes (avant de supprimer les comptes)
DELETE FROM favorites
WHERE user_id IN (SELECT id FROM auth.users WHERE email LIKE '%@book-n-pay.invalid')
   OR biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%');

DELETE FROM referral_events
WHERE referrer_id IN (SELECT id FROM auth.users WHERE email LIKE '%@book-n-pay.invalid')
   OR referred_id IN (SELECT id FROM auth.users WHERE email LIKE '%@book-n-pay.invalid');

-- 5. Dépendances du business (avant de supprimer le business)
DELETE FROM staff_absences
WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%');

DELETE FROM staff_schedules
WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%');

DELETE FROM staff
WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%');

DELETE FROM services
WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%');

DELETE FROM business_photos
WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%');

DELETE FROM business_settings
WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%');

DELETE FROM business_reviews
WHERE biz_id IN (SELECT id FROM businesses WHERE name LIKE '[TEST 16/07]%');

-- 6. Business
DELETE FROM businesses
WHERE name LIKE '[TEST 16/07]%';

-- 7. app_users (avant auth.users, sinon la ligne app_users devient orpheline
-- si aucun ON DELETE CASCADE n'est défini sur son FK vers auth.users — non
-- vérifiable depuis le repo, 0001_schema.sql absent)
DELETE FROM app_users
WHERE id IN (SELECT id FROM auth.users WHERE email LIKE '%@book-n-pay.invalid');

-- 8. auth.users : à faire de préférence via le Dashboard Supabase
-- (Authentication > Users > filtrer "book-n-pay.invalid" > supprimer)
-- ou via l'Admin API (supabase.auth.admin.deleteUser), PAS par DELETE SQL
-- brut ici — auth.users a des tables satellites gérées par Supabase
-- (identities, sessions, refresh_tokens) que l'Admin API nettoie
-- correctement et qu'un DELETE SQL direct peut laisser orphelines.
-- Si suppression SQL malgré tout nécessaire :
-- DELETE FROM auth.users WHERE email LIKE '%@book-n-pay.invalid';
```
